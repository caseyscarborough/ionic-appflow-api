query BuildStacks {
    stacks {
        id
        available
        eolDate
        friendlyName
        latest
        name
        platform
        precedence
        softwareVersions {
            name
            version
        }
        type
    }
}
